import 'package:flutter/material.dart';

class BibleStudyScreen extends StatelessWidget {
  const BibleStudyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BibleStudyScreen'),
      ),
    );
  }
}
