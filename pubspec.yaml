name: icoc_admin_pannel
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0-191.0.dev <4.0.0'

dependencies:
  firebase_cloud_firestore: ^5.0.1
  firebase_core: ^3.2.0
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_bloc: ^8.1.6
  go_router: ^14.2.0
  get_it: ^7.7.0
  injectable: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  injectable_generator: ^2.6.1

flutter:
  generate: true
  uses-material-design: true
